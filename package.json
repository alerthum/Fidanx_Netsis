{
  "name": "fidanx-netsis",
  "private": true,
  "description": "FidanX + Netsis — API ve web tek komutta",
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,green \"npm run start:dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
